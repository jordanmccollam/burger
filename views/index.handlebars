<div class="container text-light">
    <div class="row text-center">
        <div class="col-12 text-warning">
            <h1 class="display-3 customFont">EAT DA BURGER</h1>
        </div>
    </div>
    <div class="row">
        <div class="col-12">
            {{#each burgers}}
            <p class="bigFont">
                <button class="btn btn-warning customFont" id="{{id}}">DEVOUR!!!</button>
                <span class="customFont">-{{id}}-</span> 
                {{burger_name}}
            </p>
            {{/each}}
        </div>
    </div>
</div>

{{!-- Create a button that will submit the user input into the database --}}